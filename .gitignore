node_modules/
dist/
*.log
.DS_Store
# тека даних застосунку в корені проєкту — але НЕ public/data з кодом інтерфейсу.
# Через це правило файл public/data/markets.js не потрапив у репозиторій, і
# зібрані застосунки відкривалися порожніми: без нього не завантажувався
# весь код інтерфейсу.
/data/
.env
package-lock.json

# бінарники yt-dlp (тягнуться скриптом, не зберігаються в git)
bin/yt-dlp
bin/yt-dlp.exe
